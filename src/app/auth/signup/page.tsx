import { PagePlaceholder } from "@/components/page-placeholder";

export default function SignupPage() {
  return (
    <PagePlaceholder
      module="Auth"
      title="Créer un compte"
      description="Inscription — à câbler avec Supabase."
    />
  );
}
